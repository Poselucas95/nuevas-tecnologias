@model IEnumerable<GymSolutions.Models.Ejercicio_Categoria>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Categoria.catNombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ejercicio.ejNombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Categoria.catNombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ejercicio.ejNombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_EJERCICIO_CATEGORIA }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_EJERCICIO_CATEGORIA }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_EJERCICIO_CATEGORIA })
        </td>
    </tr>
}

</table>
